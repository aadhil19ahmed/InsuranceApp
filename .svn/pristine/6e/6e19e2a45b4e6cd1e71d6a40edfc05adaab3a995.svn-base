using System;
using System.Collections.Generic;

using Xamarin.Forms;

namespace ACIA.Views
{
    public partial class AdditionalFormView : ContentPage
    {
        public AdditionalFormView()
        {
            InitializeComponent();
        }

        protected override void OnAppearing()
        {
            base.OnAppearing();
            PaymentMethodPicker.WidthRequest = Section.Width / 2;
            CoverStartDatePicker.WidthRequest = Section.Width / 2;
            QuoteTypePicker.WidthRequest = Section.Width / 2;
            CoverTypePicker.WidthRequest = Section.Width / 2;
            PolicyNumberEntry.WidthRequest = Section.Width / 2;
            PolicyStartDatePicker.WidthRequest = Section.Width / 2;
            PolicyEndDatePicker.WidthRequest = Section.Width / 2;
            ExistingCoverTypePicker.WidthRequest = Section.Width / 2;
            CarUsageStack.WidthRequest = Section.Width / 2;
            CarUsedForPicker.WidthRequest = Section.Width / 2;
            AnnualMileageStack.WidthRequest = Section.Width / 2;
            AnnualMileageEntry.WidthRequest = Section.Width / 2;
            DaytimeCarParkStack.WidthRequest = Section.Width / 2;
            DayTimeUsagePicker.WidthRequest = Section.Width / 2;
            OvernightCarParkStack.WidthRequest = Section.Width / 2;
            OvernightUsagePicker.WidthRequest = Section.Width / 2;
            NoofPaidDriversPickerStack.WidthRequest = Section.Width / 2;
            NoofPaidDriversPicker.WidthRequest = Section.Width / 2;
            SumofPaidDriversPickerStack.WidthRequest = Section.Width / 2;
            SumofPaidEntry.WidthRequest = Section.Width / 2;
            SumofNamedPassengersPickerStack.WidthRequest = Section.Width / 2;
            SumofUnNamedPassengersPickerStack.WidthRequest = Section.Width / 2;
            SumofNamedPassengersEntry.WidthRequest = Section.Width / 2;
            SumofUnNamedPassengersEntry.WidthRequest = Section.Width / 2;
            NoofNamedPassengersPickerStack.WidthRequest = Section.Width / 2;
            NoofNamedPassengersPicker.WidthRequest = Section.Width / 2;
            NoofUnNamedPassengersPickerStack.WidthRequest = Section.Width / 2;
            NoofUnNamedPassengersPicker.WidthRequest = Section.Width / 2;
            UseAnothervehicleStack.WidthRequest = Section.Width / 2;
            IsAnotherVehiclePicker.WidthRequest = Section.Width / 2;
            ElecAccEntry.WidthRequest = Section.Width / 2;
            NonElecAccEntry.WidthRequest = Section.Width / 2;
            CNGFitmentEntry.WidthRequest = Section.Width / 2;
            VoluntaryExcessStack.WidthRequest = Section.Width / 2;
            VoluntaryExcessPicker.WidthRequest = Section.Width / 2;

        }

        void Handle_Clicked(object sender, System.EventArgs e)
        {
            Navigation.PushAsync(new PoliciesListView());
        }
    }
}
